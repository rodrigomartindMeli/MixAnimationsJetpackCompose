<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidutnba.motionlayoututn">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MotionLayoutUTN">
        <activity android:name=".conjunctiondemo.FullDemoActivity"></activity>
        <activity android:name=".initmotion.InitDemoActivity" />
        <activity android:name=".customattribute.CustomAttributeDemoActivity" />
        <activity
            android:name=".customattribute.CustomAttributeActivity"
            android:label="@string/title_activity_custom_attribute"
            android:theme="@style/Theme.MotionLayoutUTN.NoActionBar" />
        <activity android:name=".layout.DemoLayoutActivity" />
        <activity android:name=".propertyset.DemoPropertySetActivity" />
        <activity android:name=".transform.DemoTranformActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>